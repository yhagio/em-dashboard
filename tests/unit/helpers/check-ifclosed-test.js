import { checkIfclosed } from 'em-dashboard/helpers/check-ifclosed';
import { module, test } from 'qunit';

module('Unit | Helper | check ifclosed');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = checkIfclosed([42]);
  assert.ok(result);
});
