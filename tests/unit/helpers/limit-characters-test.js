import { limitCharacters } from 'em-dashboard/helpers/limit-characters';
import { module, test } from 'qunit';

module('Unit | Helper | limit characters');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = limitCharacters([42]);
  assert.ok(result);
});
